# Copyright 2022 zhaoarden
name: GitHub Actions CI
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      # - name: Before Deploy2 🏗️
      #   run: echo "🍏 3${{ secrets.USER_NAME }}--4${{ env.secrets.SERVER_HOST }}"
      # - name: Connect to the server🔧
      #   run: ssh ${{ secrets.REMOTE_TARGET }}@${{ secrets.SERVER_HOST }} -A
      - name: Install 🔧
        run: yarn install

      # # - name: Lint 🧐
      # #   run: npm run lint

      - name: Build 🏗️
        run: yarn build
      # - name: Create Tag Release
      #   uses: beyond5959/create-release-github@v1
      #   with:
      #     tag_name: 7e80c809f34b653a75d675520a69e3295861f68a
      #     release_name: Release 7e80c809f34b653a75d675520a69e3295861f68a
      #     draft: false
      #     prerelease: false #Error: Bad credentials


      # - name: Test 🚨
      #   run: npm test
      # 上传打包资源
      - name: SSH Deploy
        uses: easingthemes/ssh-deploy@v2.2.11
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SERVER_PRIVATE_KEY }}
          ARGS: "-rltgoDzvO"
          SOURCE: "build/"
          REMOTE_HOST: ${{ secrets.SERVER_HOST }}
          REMOTE_USER: ${{ secrets.USER_NAME }}
          TARGET: ${{ secrets.REMOTE_TARGET }}
          # EXCLUDE: "/dist/, /node_modules/"

      # - name: Deploy
      #   uses: easingthemes/ssh-deploy@v2.0.7
      #   env:
      #       SSH_PRIVATE_KEY: ${{ secrets.SERVER_PRIVATE_KEY }}
      #       ARGS: '-avz --delete'
      #       SOURCE: 'build/'
      #       REMOTE_HOST: ${{ secrets.SERVER_HOST }}
      #       REMOTE_USER: ${{ secrets.USER_NAME }}
      #       TARGET: /var/www/susieTool # 服务器目标路径

# 参考文档
# https://juejin.cn/post/7028882363606106148